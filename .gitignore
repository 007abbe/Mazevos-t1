# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env
.env
.env.*
!.env.example

# Supabase CLI link state (contains the project ref, regenerated by `link`)
supabase/.temp

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Backtest working data. The vintage cache is ~100MB of FRED revision history
# and is reproducible from the API in a few minutes; the per-session output is
# regenerated on every run. The ISM history is NOT here — it lives in
# public/data/ism_pmi.json, is committed, and is expensive to rebuild.
backtest/
